name: domine
description: Search domains with expressions. Insert numbers, letters, and any TLDs in one query.
version: 1.1.0+3
repository: https://github.com/breitburg/domine
issue_tracker: https://github.com/breitburg/domine/issues

topics:
- cli

environment:
  sdk: ^3.0.6

dependencies:
  args: ^2.4.2
  dart_openai: ^4.0.0
  dio: ^5.3.2
  dio_smart_retry: ^5.0.0
  tint: ^2.0.1

dev_dependencies:
  lints: ^2.0.0

executables:
  domine:

platforms:
  macos:
  linux:
